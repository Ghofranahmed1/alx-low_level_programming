#include <stdio.h>

/**
*main - Enter point
*
*program to print all lower cases
*
*Return: always 0 (sucsess)
*
*/

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)

	putchar(c);
	putchar('\n');
return (0);
}
