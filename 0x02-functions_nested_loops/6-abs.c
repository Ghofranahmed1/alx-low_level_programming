#include "main.h"

/**
 * _abs - unction that computes the absolute value of an integer.
 * @int - an input .
 * Return: 0 always (success)
 */

int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
